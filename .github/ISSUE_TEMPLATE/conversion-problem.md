---
name: Conversion problem
about: Understudy refused, crashed, or produced something that looks wrong
title: "[bug] <what happened>"
labels: bug
---

**Do not attach ROM images.** The manifest and the hashes are enough; if they
are not, we will ask you to run a command rather than send data.

## What you ran

```
<the exact command>
```

## What happened

```
<the full output, including any error>
```

## What you expected instead

## Your ROM set

- Game, if known:
- `understudy identify` output (it prints hashes whether or not it recognises
  the set):

```
<paste it>
```

- Device types and sizes, and how you read them:

## Manifest

If a conversion completed, please attach the `*.manifest.json` from the output
directory. It records the tool version, profile, tables, hashes, layout and
per-phrase results — and no ROM contents.

## Environment

- Understudy version:
- Python version:
- OS:
